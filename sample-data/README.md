# Fictional test data

Every person, employer, role, URL, decision, and metric in this folder is fictional. These fixtures exist only for deterministic tests and workflow development. The local dashboard never imports them. Never replace them with copied or redacted private runtime data.
